#!/usr/bin/env node
import {load,evidenceMap,failIf} from './refrigeration-fault-lib.mjs';
const data=load('refrigeration-error-codes.json'),ev=evidenceMap(),errors=[],keys=new Set();if(data.policy?.universalCodesForbidden!==true)errors.push('universalCodesForbidden must be true');if(data.policy?.controllerScopeRequired!==true)errors.push('controllerScopeRequired must be true');
for(const x of data.codes||[]){for(const k of ['manufacturer','modelScope','code','officialMeaning','sourceEvidenceId'])if(!x[k])errors.push(`error code missing ${k}`);const key=`${x.manufacturer}|${x.modelScope}|${x.code}`;if(keys.has(key))errors.push(`duplicate ${key}`);keys.add(key);if(/universal/i.test(x.modelScope||''))errors.push(`${key}: universal scope forbidden`);if(!ev.has(x.sourceEvidenceId))errors.push(`${key}: unknown evidence`);if((x.safeChecks||[]).length<2||(x.serviceOnlyChecks||[]).length<2)errors.push(`${key}: safe/service checks required`)}
for(const x of data.excludedAmbiguities||[]){if(!x.reason||!x.sourceEvidenceId)errors.push('excluded ambiguity requires reason/evidence');if(!ev.has(x.sourceEvidenceId))errors.push(`${x.code}: unknown ambiguity evidence`)}if((data.codes||[]).length<8)errors.push('at least 8 controller-scoped codes required');if(!(data.excludedAmbiguities||[]).some(x=>x.code==='Err'))errors.push('generic Err display state must remain excluded');
failIf(errors,`Refrigeration error-code registry passed: ${(data.codes||[]).length} controller-scoped codes, ${(data.excludedAmbiguities||[]).length} ambiguity excluded`);
