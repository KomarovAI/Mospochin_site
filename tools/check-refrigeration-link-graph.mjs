#!/usr/bin/env node
import {load,manifest,read,failIf} from './refrigeration-fault-lib.mjs';const graph=load('refrigeration-link-graph.json'),nodes=graph.nodes||[],pages=manifest(),errors=[];const known=new Set(pages.map(x=>x.page)),byPage=new Map(pages.map(x=>[x.page,x]));
function actual(page){const html=read(page),out=[];for(const m of html.matchAll(/<a\b[^>]*href=["']([^"']+)["']/gi)){const v=m[1].split('#')[0].split('?')[0].replace(/^\//,'');if(known.has(v)&&v!==page)out.push(v)}return [...new Set(out)].sort()}
if(nodes.length!==pages.length)errors.push(`nodes ${nodes.length} != pages ${pages.length}`);for(const n of nodes){const p=byPage.get(n.page);if(!p)errors.push(`unknown node ${n.page}`);if(n.status!==p?.status||n.indexable!==p?.indexable||n.pageType!==p?.pageType)errors.push(`${n.page}: graph/manifest mismatch`);for(const t of n.plannedOutgoing||[]){if(!known.has(t))errors.push(`${n.page}: unknown target ${t}`);if(t==='holodilniki.html')errors.push(`${n.page}: household target forbidden`);if(n.indexable&&byPage.get(t)?.pageType==='direct')errors.push(`${n.page}: organic -> Direct planned link forbidden`)}if(n.page!=='holodilnoe-oborudovanie.html'&&n.pageType!=='direct'&&!(n.plannedOutgoing||[]).includes('holodilnoe-oborudovanie.html'))errors.push(`${n.page}: link to umbrella hub required`);if(p?.status==='published'){const a=actual(n.page);if(JSON.stringify(a)!==JSON.stringify([...(n.actualOutgoing||[])].sort()))errors.push(`${n.page}: actualOutgoing differs from HTML`)}}
if(graph.rules?.maxDepthFromHub!==1)errors.push('maxDepthFromHub must be 1');if(graph.rules?.organicToDirectForbidden!==true)errors.push('organicToDirectForbidden must be true');failIf(errors,`Refrigeration planned link graph passed: ${nodes.length} nodes`);
