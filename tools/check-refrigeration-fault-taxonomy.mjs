#!/usr/bin/env node
import {taxonomy,evidenceMap,exists,failIf} from './refrigeration-fault-lib.mjs';
const rows=taxonomy(),ev=evidenceMap(),errors=[],ids=new Set(),slugs=new Set();
for(const x of rows){if(!x.symptomId||ids.has(x.symptomId))errors.push(`duplicate/missing symptomId ${x.symptomId}`);ids.add(x.symptomId);if(!x.slug||slugs.has(x.slug))errors.push(`duplicate/missing slug ${x.slug}`);slugs.add(x.slug);if(x.status==='planned'){if(x.publicationState!=='evidence_ready')errors.push(`${x.symptomId}: planned symptom must be evidence_ready`);if(exists(x.slug))errors.push(`${x.symptomId}: planned root unexpectedly exists`)}else if(x.status==='published'){if(x.publicationState!=='published')errors.push(`${x.symptomId}: published symptom must have publicationState=published`);if(!exists(x.slug))errors.push(`${x.symptomId}: published root missing`)}else errors.push(`${x.symptomId}: invalid status ${x.status}`);if(!(x.equipmentFamilies||[]).length)errors.push(`${x.symptomId}: equipmentFamilies requires >=1`);for(const k of ['userSymptoms','safeChecks','stopUseConditions','probableNodes','probableNodeLabels','technicianChecks','relatedSymptoms','sourceEvidenceIds','prohibitedClaims'])if((x[k]||[]).length<2)errors.push(`${x.symptomId}: ${k} requires >=2`);if((x.safeChecks||[]).length<3)errors.push(`${x.symptomId}: safeChecks requires >=3`);if((x.probableNodes||[]).length!==(x.probableNodeLabels||[]).length)errors.push(`${x.symptomId}: node labels mismatch`);for(const id of x.sourceEvidenceIds||[])if(!ev.has(id))errors.push(`${x.symptomId}: unknown evidence ${id}`);}
const known=new Set(rows.map(x=>x.symptomId));for(const x of rows)for(const rel of x.relatedSymptoms||[])if(!known.has(rel))errors.push(`${x.symptomId}: unknown related symptom ${rel}`);
const groups=Object.fromEntries(['general','blast_chiller','ice_machine'].map(g=>[g,rows.filter(x=>x.symptomGroup===g).length]));if(rows.length!==32)errors.push(`expected 32 symptoms, got ${rows.length}`);if(groups.general!==16||groups.blast_chiller!==7||groups.ice_machine!==9)errors.push(`group counts mismatch ${JSON.stringify(groups)}`);
failIf(errors,`Refrigeration taxonomy passed: ${groups.general} general, ${groups.blast_chiller} blast, ${groups.ice_machine} ice`);
